# address book system
第一次尝试，通讯录系统

项目简介

本项目是基于 C++ 开发的控制台通讯录管理系统，在原有纯内存存储版本基础上，改造升级为 MySQL 数据库持久化存储。实现联系人增、删、改、查、清空等完整业务功能，数据持久化保存，重启程序数据不会丢失。

一、本次更新与新增内容
接入 MySQL 数据库，实现数据持久化
新增 MySQL 数据库连接、关闭封装函数
程序启动自动从数据库加载所有联系人到内存，实现内存与数据库数据同步
新增、删除、修改、清空联系人操作，同时同步操作内存 + 数据库

全局编码统一改造
全部代码文件统一修改为 UTF-8 编码保存
控制台设置 UTF-8 输入输出编码，解决终端中文乱码
MySQL 连接统一设置utf8mb4字符集，彻底解决中文插入数据库报错问题

代码 Bug 修复
修复查找、联系人展示时，全部字段只显示姓名的赋值错误问题
规范模板类参数传参，修正数字传字符串导致的语法报错
完善所有数据库操作的错误捕获，报错时打印详细错误信息

程序交互优化
增加界面清屏、操作暂停，提升控制台使用体验
优化文字提示，功能提示、错误提示更直观易懂

隐私性可改性
将 MySQL 账号密码抽离至配置文件
`main.cpp` 开头的配置常量中修改成自己的数据库地址、用户名、数据库名或端口

二、开发遇到的问题及解决方案
1. 程序重启后数据全部丢失
问题现象：关闭重启程序，之前添加的联系人全部消失
原因：最初仅依靠数组做内存存储，无持久化能力
解决：对接 MySQL 数据库，程序启动自动读取库中数据加载到内存

2. 中文存入 MySQL 报错
问题现象：插入中文姓名，数据库报编码错误
原因：VSCode 文件、Windows 终端、MySQL 三者编码不统一，早期习惯使用 GBK 编码
解决：全部统一为 UTF-8；控制台设置 UTF-8 编码；MySQL 连接设置utf8mb4字符集

3. VSCode 右上角运行按钮无法运行项目
问题现象：Ctrl+Shift+B 可正常编译运行，右上角启动按钮直接报错
原因：下载过多vscode插件，如Code Runner,C/C++ Runner,C/C++(Microsoft)等多个独立可进行cpp的编译系统，造成配置混乱，运行按钮不会读取配置好的编译参数，无法识别 MySQL 头文件与库文件
解决：统一使用 Ctrl+Shift+B 编译 + 终端运行 的方式（Microsoft）或删除不必要插件

4. SQL 语句执行成功，数据库无数据
问题现象：控制台提示添加成功，数据表无新增数据
原因：SQL 语句引号书写格式错误，语句执行实际失败
解决：规范 SQL 拼接格式，增加执行结果判断，打印错误日志排查

三、运行环境与软件版本要求
基础软件要求
VSCode（安装 C/C++ 官方插件）
MinGW-w64（配置环境变量，保证 g++ 可全局调用）
MySQL 8.0 版本
Git（用于克隆项目）

环境基础要求
操作系统：Windows 10 / Windows 11
编译工具：g++ 支持 C++11 及以上标准

四、项目克隆后配置部署教程
克隆项目
bash
git clone 本项目仓库地址

数据库初始化
打开 MySQL，新建数据库：address_book
执行建表语句：尽量不要修改表名
sql
CREATE TABLE IF NOT EXISTS contact (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(50) NOT NULL,
    sex INT,
    age INT,
    phone VARCHAR(20),
    addr VARCHAR(100)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
修改数据库连接配置
在系统中设置环境变量 `MYSQL_PASSWORD`，值为你的MySQL密码：
    - Windows: 打开命令提示符，输入 `set MYSQL_PASSWORD=你的密码`
    - Linux/macOS: 打开终端，输入 `export MYSQL_PASSWORD=你的密码`
如需修改数据库地址、用户名、数据库名或端口，可在 `main.cpp` 开头的配置常量中修改。


VSCode 编译环境校验
确认.vscode中配置文件，已配置 MySQL 的 include、lib 路径
编译指令已携带数据库依赖：-lmysql -lws2_32
项目编译运行
VSCode 中按下 Ctrl + Shift + B 完成编译
终端进入项目 src 目录，执行运行命令：
bash
./main.exe

五、后续可优化方向（拓展展望）
增加联系人模糊查询、条件筛选功能
增添对通讯录输入信息的校验
优化初步的“内存缓存+数据库持久化”系统
完善代码异常捕获，提升程序整体容错性
优化控制台 UI 界面，美化交互展示效果
新增联系人数据导入、导出功能